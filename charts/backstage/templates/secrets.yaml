---
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: postgres-secrets
  namespace: {{ .Release.Namespace | quote }}
data:
  pg-host: {{ .Values.PG_HOST }}
  pg-user: {{ .Values.PG_USER }}
  pg-password: {{ .Values.PG_PASSWORD }}