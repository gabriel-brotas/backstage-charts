name: Trigger

on:
  registry_package:
    types: [published, updated]

jobs:
  trigger:
      runs-on: ubuntu-latest

      steps:
        - name: Trigger helmchart repo
          env:
            GITHUB_CONTEXT: ${{ toJson(github) }}
            ACTION: ${{ github.event.action }}
            PACKAGE_NAME: ${{ github.event.package.name }}
            NAMESPACE: ${{ github.event.package.namespace }}
            ECOSYSTEM: ${{ github.event.package.ecosystem }}
            PACKAGE_TYPE: ${{ github.event.package.package_type }}
            VERSION: ${{ github.event.package.package_version }}
          run: |
            echo "$GITHUB_CONTEXT"
            echo $ACTION
            echo $PACKAGE_NAME
            echo $NAMESPACE
            echo $ECOSYSTEM
            echo $PACKAGE_TYPE
            echo $VERSION
            
            # event_payload_data() {
            #   cat <<EOF
            # {
            #   "event_type": "global-backstage.new_image", 
            #   "client_payload":{
            #     "ref": "${{ github.ref }}",
            #     "repository": "${{ github.repository }}",
            #     "actor": "${{ github.event.pusher.name }}"
            #   }
            # }
            # EOF
            # }
            # curl -X POST \
            # -H "Accept: application/vnd.github.v3+json" -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            # https://api.github.com/repos/wexinc/global-backstage-helmcharts/dispatches \
            # --data "$(event_payload_data)"   
